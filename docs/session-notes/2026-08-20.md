# 2026-08-20

- 23:55 main 承認ゲート付き自動ブログ基盤のレビュー指摘修正一式。初期実装（36fa748）に対するレビュー（Tier 2＋UX）の指摘12件を解消: revise-article.ps1へのInvoke-Git導入（git/gh失敗の無言続行を排除・差し戻し導線の信頼性確保が最重要のMajor）、404ページ新設、記事日付とファイル名の整合検査、affiliateフラグ整合検査、週次実行のログ出力（cmd.exe経由のOSレベルリダイレクト。PowerShell 5.1の`*>>`はネイティブコマンドのstderrをNativeCommandError化するため不採用）、kramdown hard_wrap無効化、漢数字規約の3層（CLAUDE.md・生成プロンプト・validate）統一。検証は陽性対照方式（新設検査に違反サンプルを伏せて通し検出を確認）。未完了: リモートリポジトリ作成・push・Pages有効化・PR一巡テスト（gh認証済み、この後実施）。

- 24:50頃 main リポジトリ公開（YuuMinaduki1080/totonou-desk）・Pages有効化・サイト表示確認まで完了。一巡テストはclaude CLIのOAuth失効で停止（validateがエラー文記事を検出しpush/PRを正しく阻止＝機械検査の実働確認になった）。是正: generate/revise両スクリプトのclaude呼び出し直後に終了コード＋front matter開始のサニティチェックを追加（エラー文を記事として保存・上書きしない）。Tier 1レビュー（opus 1名）でAPPROVE×2。残: claude CLI再認証（ユーザー作業）→一巡テスト再実行→週次タスク登録。

- 25:40頃 main claude再認証後の一巡テストで第2の不具合を実測: 長い日本語プロンプトの引数渡しでclaudeが「ご依頼の内容が読み取れませんでした」と応答（ガードが正しく停止）。プロンプトを標準入力渡しに変更し$OutputEncoding/[Console]::OutputEncodingをUTF-8固定（生成・修正の両スクリプト）。修正後の一巡テストは成功し、記事生成→validate通過→push→PR #1作成まで到達。Tier 1レビューAPPROVE（コメント精度の完成文を逐語適用）。残: ユーザーのPR承認体験→週次タスク登録（登録後にStart-ScheduledTaskで1回手動起動しログ確認＝レビュアー申し送り）。

- 27:10頃 main リポジトリをOrganization totonou-desk-jp へ移管し totonou-desk-jp.github.io にリネーム（ユーザー指示: 仕事用の所有者表示へ変更。org名はドット不可のためtotonou-desk.jp入力がハイフンに正規化されたもの）。url/baseurlをルート配信に変更（1ae3af7）。新URL https://totonou-desk-jp.github.io/ でビルド成功・新トーンのリード文・広告表記・ルート直下の記事リンクを実測確認。PR #1も新リポジトリへ引き継ぎを確認。旧URLはリダイレクトされず消滅（リンク資産ゼロのため実害なし）。

- 26:30頃 main ブランドトーン改定（ユーザー指示:「静か」系→整理整頓・ミニマリズム×サウナの「ととのう」心地よさ・チル感）。トップリード・サイト説明・運営者情報・規約・生成プロンプトの7箇所＋_config.ymlのurl設定。Tier 2レビュー（正確性/規約整合の2レンズ）で、url設定に伴う段階0「未完了」記述の偽2件・SNSチャネル列挙の事実齟齬等を追加検出し是正。about.mdのSNS表記は列挙をやめ@totonou_desk表記（ユーザー追認済み）。index.mdのdescriptionは_config.ymlへ一本化。

- 28:20頃 main PR #1マージ→記事公開（サイト2本目）・週次タスク登録（日曜21時・Ready）。スケジューラ実働検証で第3の故障を実測: 非対話claudeがプロンプトの「出力先」を見て自らファイル作成を試み書き込み許可で停止（ガードが検知・無傷）。是正: プロンプトに「ファイル作成ツール不使用・本文のみ出力」を明示し、affiliates.ymlへの追記指示（非対話では履行不能）を「無いキーは使わない」へ反転。Tier 1レビュー2巡（Major1解消・完成文逐語適用）でAPPROVE。

- 29:30頃 main スケジューラ実働検証4回: いずれも安全停止（①claudeのファイル作成試行→プロンプトで是正済み ②③本文の下限割れ2,268字・2,499字→文字数指定を2,700〜3,800字へ締めて是正済み ④front matterの---フェンス欠落=形式ゆらぎ）。基盤（cmd経由ログ・文字コード・ガード・検証）は全て設計どおり動作。生成の形式・文字数の順守率が非対話実行で不安定という残課題を確認。次セッションへの申し送り: generate-article.ps1に生成リトライ（上限2回程度）を追加し、必要なら出力先頭の正規化を検討→スケジューラ検証を通してPR #2作成を確認する。

- 23:49 main 申し送りの是正を実装。ログ再調査で④の実態は「---フェンス欠落」ではなく```markdownフェンス包み（内側のfront matterは正常）と判明。generate-article.ps1にRemove-WrappingCodeFence（先頭空白・先頭フェンス除去、先頭を除去した場合のみ末尾フェンス除去）と最大3試行の生成リトライを追加。環境起因（終了コード非0・空出力）は再試行で直らないため即throwとし「認証状態を確認」文言をその分岐だけに残す。品質起因はvalidateのNG文言をそのまま次試行プロンプトへ付加。revise-article.ps1には正規化＋書き分けのみ複製（手動実行で人が再実行できるためリトライ不採用）。runbookへ全滅時の手動再実行と同日再実行時のdraftブランチ削除の導線を追記。pipeline Tier 2でMajor1件（runbook文言）修正後、全レンズAPPROVE（モック検証: 正規化7ケース・リトライ遷移4シナリオ・revise3モード全合格）。残Minor（.DESCRIPTIONのループ未反映）はAPPROVE終結。残: 実claude -SkipPush確認→スケジューラ本番起動→PR #2確認。
