# 2026-08-20

- 23:55 main 承認ゲート付き自動ブログ基盤のレビュー指摘修正一式。初期実装（36fa748）に対するレビュー（Tier 2＋UX）の指摘12件を解消: revise-article.ps1へのInvoke-Git導入（git/gh失敗の無言続行を排除・差し戻し導線の信頼性確保が最重要のMajor）、404ページ新設、記事日付とファイル名の整合検査、affiliateフラグ整合検査、週次実行のログ出力（cmd.exe経由のOSレベルリダイレクト。PowerShell 5.1の`*>>`はネイティブコマンドのstderrをNativeCommandError化するため不採用）、kramdown hard_wrap無効化、漢数字規約の3層（CLAUDE.md・生成プロンプト・validate）統一。検証は陽性対照方式（新設検査に違反サンプルを伏せて通し検出を確認）。未完了: リモートリポジトリ作成・push・Pages有効化・PR一巡テスト（gh認証済み、この後実施）。

- 24:50頃 main リポジトリ公開（YuuMinaduki1080/totonou-desk）・Pages有効化・サイト表示確認まで完了。一巡テストはclaude CLIのOAuth失効で停止（validateがエラー文記事を検出しpush/PRを正しく阻止＝機械検査の実働確認になった）。是正: generate/revise両スクリプトのclaude呼び出し直後に終了コード＋front matter開始のサニティチェックを追加（エラー文を記事として保存・上書きしない）。Tier 1レビュー（opus 1名）でAPPROVE×2。残: claude CLI再認証（ユーザー作業）→一巡テスト再実行→週次タスク登録。

- 25:40頃 main claude再認証後の一巡テストで第2の不具合を実測: 長い日本語プロンプトの引数渡しでclaudeが「ご依頼の内容が読み取れませんでした」と応答（ガードが正しく停止）。プロンプトを標準入力渡しに変更し$OutputEncoding/[Console]::OutputEncodingをUTF-8固定（生成・修正の両スクリプト）。修正後の一巡テストは成功し、記事生成→validate通過→push→PR #1作成まで到達。Tier 1レビューAPPROVE（コメント精度の完成文を逐語適用）。残: ユーザーのPR承認体験→週次タスク登録（登録後にStart-ScheduledTaskで1回手動起動しログ確認＝レビュアー申し送り）。
